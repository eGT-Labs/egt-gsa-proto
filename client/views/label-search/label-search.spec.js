'use strict';

describe('Controller: LabelSearchCtrl', function () {

  beforeEach(module('egtGsaProto'));

  var LabelSearchCtrl;

  beforeEach(inject(function ($controller) {
    LabelSearchCtrl = $controller('LabelSearchCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
